<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">Client: {{ name }} <button {{action "edit"}}>Edit</button></h3>
    </div>
    <div class="panel-body">

    {{#if isEditing}}
        {{partial 'client/edit'}}
    {{else}}
        {{partial 'client/show'}}
    {{/if}}

    </div>
</div>
